// const defaultHost =
//   import.meta.env.NODE_ENV === 'development' ? '' : import.meta.env.BACKEND_URL || location.origin
// const baseURL = defaultHost + `${import.meta.env.API_URL}`

// const API = new APImodule({ baseURL })

// export default API
